name: Keepalive

on:
  schedule:
    - cron: '*/50 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Prod /api/crime/stats
        run: curl -s -o /dev/null -w "HTTP狀態：%{http_code}\n" "https://taipei-crime-map-prod.ambitioussand-7326440b.japaneast.azurecontainerapps.io/api/crime/stats"
